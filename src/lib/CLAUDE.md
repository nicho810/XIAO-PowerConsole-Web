# lib/
> L2 | 父级: /src/CLAUDE.md (待创建)

通用工具库 — 与业务无关的数据结构与格式化工具

## 成员清单
- `ring-buffer.ts`: RingBuffer 定容环形缓冲区 (Float64Array)，默认 3000 容量，O(1) push
- `utils.ts`: formatVoltage/formatCurrent/formatPower 物理量格式化 + cn() 类名拼接

[PROTOCOL]: 变更时更新此头部，然后检查 CLAUDE.md
