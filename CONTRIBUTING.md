# 参与贡献

## 开发

使用 Node.js 22/24 与 npm，执行 `npm ci`。不要提交其他包管理器锁文件、测量 CSV、设备序列号或本地环境文件。

## 问题反馈

在仓库 Issues 中提供：操作系统、浏览器版本、设备与固件版本、复现步骤、预期/实际结果，以及去除私人信息后的日志。无硬件问题请先尝试 `/tests/preview.html`。

## Pull Request

说明具体问题和行为变化，UI 修改附截图。提交前执行 `npm test`、`npm run build` 和 `npm audit --audit-level=moderate`。协议、串口与积分变化应有对应行为测试；真机结果注明环境，不把模拟验证写成硬件验证。

遵循 [AGENTS.md](AGENTS.md) 的模块边界及文档约定，保持高频采样与 React 渲染解耦。只提交与问题相关的改动。

## 贡献许可

提交贡献时，请确认你有权提供这些代码，并同意贡献以本项目的 GPL-3.0-only 许可分发。引入第三方代码时保留其版权及许可声明，确认其许可与本项目兼容。
